# CLAUDE.md

@AGENTS.md

<!-- The line above is a Claude Code IMPORT, not a link. It inlines AGENTS.md
     into this session's context.

     Do not "tidy" it into a markdown link. It was a link until 2026-08-31, and
     a cold-session test that day found Claude Code was the ONLY one of the
     three agents that could not answer questions from AGENTS.md - Codex and
     Antigravity both auto-received it, while Claude Code loaded this stub,
     read "see AGENTS.md" as prose, and never opened the file. A pointer an
     agent does not follow is not an entry point. -->
