In this video, we'll go in detail into how to download and set up the FreeCAD connector for Claude and then put it to work modeling some real parts. Claude can now connect to FreeCAD opening up infinite possibilities for engineers, hobbyists, makers, and CAD designers alike. If you haven't seen it yet, we covered what this connector can actually do in our previous video. So, before we dive in, let's quickly look at how this actually works. When you give a prompt, Claude analyzes it and reasons through what needs to be done. It then connects to FreeCAD through the MCP connector and executes the corresponding FreeCAD code through the Python API to create the part. FreeCAD sends feedback back a screenshot of the viewport again through the MCP connector. Claude analyzes that result and based on what it sees, sends the next command. This loop keeps repeating, which is what lets Claude build up a part step-by-step. This video consists of five simple steps and we'll go into detail on each one, so you can install everything successfully. It's really easy to follow, just stick with me. Before we jump in, I've attached a free PDF in the description. You can follow along step-by-step. It has all the clear instructions, exact command lines, and direct links you'll need, so you don't have to pause and search for anything yourself. Keep it open alongside this video as we go. Step one, installing Claude desktop. First, head over to claude.ai and download the desktop app. The link is also in the attached PDF. I'm on Windows, so I'll click download for Windows, but if you're on a Mac, just choose that version instead. Once it's downloaded, open the installer. It'll set itself up automatically. When that's done, sign in to your Claude account. And you're ready for the next step. Step two, installing FreeCAD. Now, let's install FreeCAD itself. Visit the official site freecad.org. The link's in the PDF as well. Click download. Then, choose the installer that matches your operating system. For me, that's the 64-bit Windows installer. Run through the setup. Finish, and FreeCAD is installed. At this point, both Clawd and FreeCAD are on your machine, but they can't talk to each other yet. Now, if you open Clawd desktop and click on the plus button and navigate to connectors, you won't see a FreeCAD option. And if you open FreeCAD's workbench drop-down, there's no MCP add-on, either. Step three, installing UVX. To install FreeCAD MCP, we first need a tool called UVX, a Python package manager and tool runner that lets you run Python CLI tools instantly without a full installation process. To install UVX on Windows, first open the start menu and search for PowerShell. Once it shows up, right-click it and select run as administrator, or just click run as administrator directly from the search results. PowerShell will now open. Next, open the attached PDF I mentioned earlier and scroll to the UVX installation section. Copy the command line shown there. Switch back to PowerShell and make sure your current directory is set to Windows system32. This is usually the default when PowerShell opens as administrator. If you'd prefer to grab the command straight from the source instead of the PDF, you can visit the official UV website. The link's provided as well. Go to their installation page, switch the platform to Windows, and copy the same command from there. Now, paste the command into PowerShell and press enter. PowerShell will download and install UVX automatically. And once it's done, you'll see a message confirming everything installed successfully. To verify the installation, let's run a version check. But there's one important step first. Close PowerShell completely and reopen it. If you run the version check without restarting PowerShell, it'll return an error. So, open start, search PowerShell again, right-click it, and select run as administrator. PowerShell opens fresh. Go back to the PDF and copy the version check command listed there. This checks which version of UVX is installed. If everything installed correctly, it'll return a version number. Switch back to PowerShell, paste the command, and press enter. It should return the version number, confirming UVX is successfully installed. With that done, we're ready to move on to the next step. Step four, installing the FreeCAD MCP server. Now, let's install the actual FreeCAD MCP server. Open the attached PDF again and click through to this page, github.com/necanat/freecad-mcp. This repository is the FreeCAD MCP built by necanat. It's what allows Cloud Desktop to control FreeCAD directly. On that page, click the green code button, then click download zip. Once it's downloaded, extract the zip to a folder. Open that extracted folder. Then open the add-on folder inside it. You'll find a folder named FreeCAD-MCP. Copy that entire folder. Now hold Windows key plus R to open the run dialog. Type percent appdata percent into the field and click okay. This opens your appdata roaming folder. Inside there open the FreeCAD folder and then open the v1-1 folder inside it. If there's already a mod folder in here, open it. If there isn't one create a new folder. And name it exactly mod. Then open it. And paste it inside this mod folder. One quick note. If you're using a FreeCAD version earlier than 1.1, the mod folder goes directly inside the main FreeCAD folder, not inside of subfolder. With that done, open FreeCAD again. And check the workbench drop down. You should now see the MCP add-on listed. That confirms it installed correctly. Select the MCP add-on workbench. You'll see a new toolbar appear with options like start RPC server, stop RPC server, and auto start server. Now, click start RPC server. If Windows asks for network access permission, click allow. FreeCAD and the MCP server are now successfully linked. Next, we need to connect Claude to this MCP server. Open Claude desktop, which we installed earlier. Once it's open, go to settings. You can get there quickly with control plus comma. The settings panel opens. Now go to the developer tab. This opens the local MCP servers page. If you've installed any MCP servers before, you'll see them listed here. Click the edit config button. This takes you directly to Claude's configuration file. It's a dot JSON file located in Claude's application data folder. Right click that file and select open with notepad. The file opens. Don't be intimidated by all the syntax you see. We're not writing anything from scratch here. Now, open the downloaded PDF. You'll see two blocks of code inside it. We're going to paste one of these configurations into the configuration file. This tells Claude desktop to launch the FreeCAD MCP server using a tool called UVX, which handles the package automatically. Here's the difference between the two blocks. The first block is the standard setup. After Claude runs a command in FreeCAD, it gets back both the text result and an image, a screenshot of the FreeCAD viewport. This means Claude can actually see what it built and react to it visually. It does consume more tokens per operation, though. The second block adds a flag called only text feedback. With this, Claude skips the screenshot entirely and only receives text, object names, dimensions, success, or error messages. No image, but significantly fewer tokens used per operation. If you're doing short sessions, building just one or two parts, go with the first block. If you're running longer sessions with a lot of back-to-back operations, the second one keeps things more efficient. For most beginners, I'd recommend starting with the first. You can always switch later by editing this one line. I'm going with the first block. In the configuration file, find the last but one closing curly brace. Add a comma right after it. Press enter and paste the block of code in. The important part to get right, add the comma, then press enter before the last closing curly brace. If you get that placement wrong, the file won't load correctly. Once that's done, save the text file and close Notepad. Now, fully close Cloud Desktop. Make sure to close it from the system tray as well, not just the window. If it's still running in the background, your configuration changes won't take effect. With the configuration saved and Cloud fully closed, we're ready to bring everything online. Step six, starting the connection and testing it. Let's start the connection and put it to the test. First, start FreeCAD. Go to the workbench drop-down, select the MCP add-on, and click start RPC server. And you can minimize FreeCAD now. Now, open Cloud Desktop. Click the plus icon. Go to connectors and scroll down. You'll see the FreeCAD connector listed and live. If it's toggled off for any reason, turn it on. There's another way to confirm everything's running correctly. Go to settings, control plus comma, then developer. Under local MCP servers, you'll see FreeCAD listed as added and running. That's it. We've successfully connected FreeCAD to Claude. From here, you can create almost anything in FreeCAD just by describing it in plain language. Let's put it to the test. Step seven, testing the connector. To follow along easily, I've arranged both windows side by side, so we can watch what's happening in both live as we go. Let's write our first prompt. Create a new document in FreeCAD called test part. The first time each tool runs, Claude will ask for permission to use it. I prefer clicking always allow. FreeCAD creates the document and names it test part successfully. Now, let's create a simple box. I'll prompt, create a box in FreeCAD with length 50 mm, width 30 mm, height 20 mm. It asks for permission again to create the object. I click always allow. The box is created and the view automatically adjusts to isometric, so we can see it clearly. Claude also gives us feedback in text. The box was created inside test part with the given dimensions sitting at the origin. It follows up on its own asking, "Want me to round the edges with a fillet or chamfer, hollow it out, drill a hole, or add another solid next?" Fillets can be a bit troublesome in FreeCAD, so let's test that specifically. I'll prompt "Fillet all edges." deliberately without mentioning a radius, just to see how it handles the ambiguity. Claude thinks for a moment, then asks, "What radius would you like for the fillet?" It even offers a suggestion on its own. It recommends keeping the radius under 10 mm to avoid geometry errors. It gives me a few options to choose from, and I'll go with 2 mm. After one more permission prompt, it applies the fillet cleanly across all 12 edges. Next, let's add some holes. Again, I'll keep the prompt intentionally vague. "Make four holes on top." Claude asks for the missing details, hole diameter, which I set to 4 mm, and depth, where I choose through all. The holes are created, but here's the interesting part. I never told Claude where to place them. It decided that entirely on its own. 10 mm inset from the short edge and 8 mm from the long edge. It explains that this positioning clears the fill it edges comfortably. That's reasoning through the geometry like an actual CAD engineer would. This entire design stays parametric. Fill it radius, whole diameter, whole position, every bit of it can still be changed later just by prompting. Now, let's push this a level further. Instead of typing out dimensions, we'll hand FreeCAD a rough hand-drawn sketch instead. I'll upload a sketch of a simple three-step staircase style part. Drawn from the front, top, and right side views with all the necessary dimensions marked on it. The prompt is intentionally rough, too. Create this part in FreeCAD. And in just about 20 seconds, FreeCAD builds the entire part directly from that sketch. Let's verify the accuracy. To measure dimensions on the created part, go to tools, then select measure. I'll measure the distance between these two faces. It measures exactly 36. I'll check a couple of the other dimensions, too. And they all match perfectly. Let's try one more part. But this time, we'll give Claude every detail up front instead of leaving anything for it to decide. Design a flange in FreeCAD with a base diameter of 100 mm, thickness 10 mm, and a center hole of 20 mm diameter with four bolt holes of 8 mm diameter equally spaced at 70 mm PCD. Because every parameter is already specified, Claude builds the part in a single pass. No clarifying questions needed this time. Here's something important to know before you get too deep into a session. I enter the prompt, close all the documents. Claude closes every open document, but without asking or warning first. Only after they're all closed, does it add a note. One heads-up, if any of those had unsaved changes, that work was closed without saving. Let me know if you need to recreate anything. In other words, none of those documents were saved, and there was no warning before they were closed. Claude can recreate the parts again if you ask, but that means redoing the work, and it costs additional tokens to regenerate. So, when you're using the FreeCAD connector, make it a habit to save your documents manually or explicitly prompt Claude to save them before moving on or closing anything. Let's try something that requires real mathematical and geometric calculation, a gear. I don't have FreeCAD's gears workbench installed, so Claude can't just pull a ready-made gear template. It has to build one from scratch. I prompt, design a spur gear in FreeCAD with 20 teeth, module two, face width 20 mm, and a center bore of 10 mm diameter. Claude responds that since the gears workbench isn't available, it'll need to construct an involute spur gear manually using basic geometry instead. After running through a series of calculations, it successfully builds the gear. And that's the full setup from installing Claude desktop and FreeCAD to linking them through the MCP server. If you followed along with the PDF, you should have this fully working on your own machine right now. In the next video, we're going even further building a parametric Skadis pegboard from a hand-drawn drawing, a planter pot from an AI-generated image, and a fully parametric compartment organizer all through prompts alone. If this helped you, hit subscribe so you don't miss it, and I'll see you in the next one.