# Digital Toolchain — Change Log

Editorial history for [[18_Digital_Toolchain/Digital_Toolchain_Guide|Digital Toolchain]] and its `analysis/` pages. Not reader content.

- **2026-09-13 — Folder created, and `Drawing_Conventions_From_Practice.md` moved into it from `00_Master/`.** Per an explicit owner decision, after the owner asked whether this source class needed its own knowledge-base folder.

  **What this revisits.** The [2026-09-08 Group C scoping pass](../../_Inbox/planning/design_toolchain_sources_triage_20260908.md) §5 asked exactly this question and answered `00_Master/`, on the sound reasoning that *"the taxonomy is a taxonomy of the renovation, not of the project's paperwork"* and that `00_Master/` was already where the project's own method lived. **That decision was made when the class had zero processed sources.** It had eleven by 2026-09-11, with a second batch arriving.

  **Three things changed, and two of them are mechanical rather than editorial:**
  1. **⚠️⚠️ `00_Master/` IS EXPLICITLY EXCLUDED FROM THE PAGE-SIZE GATE.** `tools/check_page_sizes.py`'s `NUMBERED_FOLDER_RE` is `^(?!00_)\d{2}_` — it skips `00_Master` by design, as project docs rather than wiki pages. **So `Drawing_Conventions_From_Practice.md` had been growing ungated**, reaching 243 lines past the ~300 soft target with no fragmentation check ever run against it, while every comparable page in the vault was gated. Moving it into a numbered folder puts it inside the backstop and the FRAGMENTED check for the first time. **This was the decisive argument, and it was not visible in 2026-09-08.**
  2. **`00_Master/` had accumulated four distinct kinds of document** across 33 `.md` files — the project's own decisions (`Design_Concept`, `Family_Requirements`, `project_decisions`), model and geometry status (`V0_*`, `Model_and_Views`, `Wall_Model_Closure_Check`), working discipline (`Evidence_Reading_Discipline`, `Validator_Design_Discipline`, `wiki_page_format`), and general toolchain knowledge — with nothing in the folder distinguishing them.
  3. **The vault already had the precedent for the split.** `17_Design_and_Ergonomics/` holds general practitioner design knowledge while `00_Master/Design_Concept.md` holds this apartment's own palette. The same line applies here, and drawing it makes the folder's scope self-policing.

  **The split as adopted.** `18_Digital_Toolchain/` = **general, source-derived** knowledge: AI workflows over construction data, 2D plan production and drawing conventions, 3D modelling and visualisation, cost and quantity take-off, tool interchange. **`00_Master/` keeps this project's own toolchain decisions and status** — `Model_and_Views.md`, `Planning_Project_Deliverable_Set.md`, `Sheet_Production_Roadmap.md`, `Revit_AutoCAD_Integration_Strategy.md`, `How_To_View_Outputs.md`, the `V0_*` pages, and the `_Inbox/planning/` gap analyses. **Nothing but `Drawing_Conventions_From_Practice.md` moved.**

  **Numbered (`18_`) rather than underscore-prefixed**, because `AGENTS.md` reserves the underscore prefix for cross-cutting *infrastructure* that is "not a topic" (`_Sources/`, `_Archive/`, `_Drawings/`, `_Knowledge/`). This behaves exactly like `17_` — practitioner attributions, extraction notes in `_Sources/`, a source-notes page, content gathered from external sources. It is a topic.

  **Link handling follows the `_supporting` dissolution precedent** (2026-08-30): **live cross-references were rewritten; historical records were left as written**, because they record what was true when they were written. Rewritten: `00_Master/Evidence_Reading_Discipline.md`, `00_Master/Planning_Project_Deliverable_Set.md`, `data/layout_rules/rules.jsonl`, `_Inbox/planning/youtube_channel_queue.md`. **Deliberately left pointing at the old path**: `_Knowledge/store/Change_Log.md`, `00_Master/processed_sources.csv`, the two triage documents, `deep_research_review_geometry_20260911.md`, and the two `_Sources/` extraction notes — all of which are dated records of a past state. The moved page's own relative links were re-depthed from `../` to `../../`.

  **⚠️ One thing a folder does not fix, and it needs a separate change**: the intake wrapper's 34-bucket taxonomy is entirely renovation-domain, so toolchain content has no bucket and falls to `Other / Unclassified`. Buckets added to `.agents/skills/renovation-knowledge-intake/SKILL.md` in the same session — see that file's own history.

- **2026-09-13 — AI toolchain batch, Round 1: 11 videos, 56 new facts, yield 5.1. Three new `analysis/` pages.** Triage: [`ai_toolchain_sources_triage_20260913.md`](../../_Inbox/planning/ai_toolchain_sources_triage_20260913.md). Pages created: `AI_Reading_Construction_Drawings.md`, `AI_For_Concept_And_Visualisation.md`, `Agent_Connected_CAD.md`. **FIVE THINGS WORTH CARRYING:**
  1. **⚠️⚠️ NINE OF ELEVEN SOURCES WERE ENGLISH, AND THE LANGUAGE RULE BIT IN THE OPPOSITE DIRECTION TO USUAL.** This vault's standing rule 1 exists to stop an auto-translated ENGLISH track being taken for a Russian source. Here the same rule required `--languages en` per video, because the default `ru,en` would have pulled auto-translated RUSSIAN captions for nine English-spoken videos. **Every transcript was fetched in its original spoken language and the `language` field was verified per file after fetching.** → **The rule is "original language", not "Russian" — and a batch can invert which way it points.**
  2. **⚠️⚠️ A SOURCE CLOSED A GAP THIS VAULT HAD EXPLICITLY RECORDED AND LEFT OPEN.** `YT_3tAYEJTyUFY` was routed on 2026-09-11 with the complaint, in its own CSV row, that it had *"NO ACCURACY MEASUREMENT of any kind."* `ItW-ielFvGg` is the same practitioner's same workflow two months later **with a 44-question benchmark: 86% / 98% / 100% accuracy at 104,000 / 66,000 / 1,400 tokens.** → **Recording precisely what a source FAILED to provide is what made the follow-up recognisable as the answer.**
  3. **⚠️⚠️ THE MEASURED RESULT SPLITS IN A WAY THE HEADLINE HIDES.** The vendor sells "50x less tokens, 20% more accurate" as one number. **The vector extraction buys nearly all the accuracy (86 → 98%); the database buys nearly all the cost reduction (66,000 → 1,400) and only two points of accuracy.** → They are separable purchases, and which one matters depends on whether your problem is being wrong or being expensive.
  4. **⚠️⚠️ AN OUTSIDE PRACTITIONER INDEPENDENTLY RESTATED THIS PROJECT'S OWN STANDING RULE 9 — AS A WAY OF CATCHING A MACHINE'S MISREADING.** The order-of-magnitude cross-check (a cable tray measured at 7 m, where another drawing gives the building as 35 m and the run spans it, is *clearly wrong*) is chain closure, reached with no connection to this project. **The strongest external validation `Evidence_Reading_Discipline.md` has.** Two more arrived the same way: an agent-connected Revit failing at spatial logic while succeeding at transcription corroborates the silently-supplied-values rule, and a practitioner reverse-engineering IFC by hand argues for reading the standard rather than inventing fields.
  5. **⚠️ A TRIAGE CORRECTION WORTH KEEPING: A ONE-MINUTE VIDEO PREDICTS LOW VOLUME, NOT LOW VALUE.** `sujS9Mgveo4` was flagged a likely skip on the title-skim (60 seconds, "perfect 3D" in the title). It was fetched anyway and **turned out to hold the only measured dimensional-fidelity check in the batch** — 610 drawn against 616 modelled — answering the open question the 35-minute masterclass left hanging. **The masterclass measured nothing.**
  **Also: one source was routed as a DOCUMENTED ANTI-PATTERN rather than as technique.** `EibFZPrtAp0` uploads a plan image to a chat model, asks for "all the dimensions... as copy-paste format for AutoCAD", and pastes the result straight into the drawing **with no verification step of any kind** — precisely the failure every other source in the batch measures or designs around. Recorded as what not to do, the same way this vault records a self-flagged live-wiring demonstration. **And every model name, version, ranking and free-tier claim across all eleven sources was deliberately discarded**, per the standing rule that capability verdicts date within months.

- **2026-09-13 — Toolchain Round 2: 11 videos, 77 new facts, yield 7.0. One new `analysis/` page; four existing pages extended.** The Round 2 explicitly named in the 2026-09-11 triage, plus the Tier 2 singles deferred on 2026-09-08. Triage and outcome: [`ai_toolchain_round2_20260913.md`](../../_Inbox/planning/ai_toolchain_round2_20260913.md). New page: `Quantity_Takeoff_and_Cost_Join.md`. **SIX THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THE DEFERRED TIER OUTYIELDED THE TIER THAT WAS PROCESSED FIRST — 7.0 against 5.1.** Both earlier triages tiered these items below the ones read first, on defensible reasoning. **Two of the three lowest-rated items in the batch were among the five richest**: `PVXE79HM0-c` was predicted "probably a listicle" and delivered a working shop's full deliverable set (yield 10); `9-hQsyWSnm4` was characterised as "wall-tracing over a scaled underlay" and delivered two modelling conventions this project does not state anywhere (yield 9). **→ A triage's confidence about a source it has not fetched is worth less than it feels. Deferral order should track COST, not predicted yield.**
  2. **⚠️⚠️ A PRACTITIONER PERFORMS THIS PROJECT'S OWN CHAIN-CLOSURE CHECK AND THROWS THE RESULT AWAY.** Drawing a flat's perimeter by typing measured dimensions, the Omsk finisher notes that on returning to the start «размер последней линии может немножко отличаться» — and dismisses it: «такие погрешности не очень страшные». **He also names the cause without connecting it: the software forces 90° corners and real flats are rarely square, so the residual is absorbing the out-of-square plus every measurement error into one segment.** **It is a free survey-quality metric being read as a nuisance — the clearest external illustration of why standing rule 9 exists and why this project gates closure rather than eyeballing it.**
  3. **⚠️⚠️ AN OUTSIDE TOOL EXPOSED A GAP IN THIS PROJECT'S OWN COST-ENGINE DESIGN.** Both take-off extensions carry **waste and tax as first-class fields on every cost line**; the gap analysis's design mentions neither. **And the vault already held the better answer to half of it** — `Bill_of_Quantities_and_Procurement.md` §5a-quinquies achieves **under 1% waste by nesting cuts** where the tool would apply a flat 7%. **The synthesis neither source states: waste is a function of whether the material NESTS and whether you control the cutting — a per-resource property with a method attached, never one global rate.**
  4. **⚠️⚠️ A THIRD AGENT-CAD ARCHITECTURE, AND THIS PROJECT IS ALREADY ON IT.** Open-loop emits geometry and cannot check it; closed-loop emits geometry and checks it by screenshot; **tool-driving delegates to a parametric tool that enforces the invariants by construction — "a wall tool cannot produce walls that fail to merge at a corner."** `export_v0_dxf.py` is exactly that. **External corroboration of a choice already made, from someone offering it as a prediction.** The same source **names the corner-merge defect `check_wall_junctions.py` exists to reject.**
  5. **⚠️ THE ELECTRICAL BOM NEEDS TWO QUANTITIES PER LINE, NOT ONE.** A conductor has **exactly one** cable type; a containment is **many per line simultaneously** (a cable in гофра passing through a wall in a гильза). **Cable length and containment length are different numbers** — a 12 m circuit might be 12 m of cable, 7 m of гофра, 4 m of штроба and 0.4 m of гильза, all separately bought and separately paid. **`ELE-01` is recorded as unmeasured; this is the schema it needs.** And **wall material is a rate driver, not a drawing colour** — chasing brick, chasing concrete and fixing to plasterboard are different operations.
  6. **⚠️ THREE INDEPENDENT ARRIVALS AT WHAT A COST MODEL IS FOR.** A modeller abandoned a build on a **deliberately incomplete** estimate ($5,300 with no roof, siding or interior); a finisher states twice that the model exists so mid-renovation changes are cheap to re-price; and this project's own gap analysis concluded the model must "hold quantities stable enough that a substitution re-prices correctly." **→ Precision is not what makes an estimate useful. Re-derivability is.**
  **Also**: one source (`YkHGQPfZEgM`) had its **styling recipe deliberately not routed** — this project's sheets are a working deliverable, not a portfolio piece — keeping only three workflow rules. One (`tJSS-IWrJoE`) was honoured as a **near-skip against a conditional written before it was fetched**. **Every model name, version and ranking across all eleven was discarded**, per the standing dating rule. **No prices carried from any source** — US 2020/2025 and Omsk 2020, all uncomparable under standing rule 2. **No regulatory content.**

- **2026-09-13 — @ConstructIQ Tier 1 round: 5 videos, 59 new facts, yield 11.8 — the highest of the three rounds in this folder** (5.1 / 7.0 / 11.8). New page `AI_Workflow_Systems.md`; `Quantity_Takeoff_and_Cost_Join.md` and `Bill_of_Quantities_and_Procurement.md` extended. Triage that authorised it: [`constructiq_channel_triage_20260913.md`](../../_Inbox/planning/constructiq_channel_triage_20260913.md). **FIVE THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THE HIGHEST YIELD OF ANY ROUND HERE CAME FROM THE MOST CONCENTRATED SOURCE, AND THAT IS A TENSION TO STATE RATHER THAN CELEBRATE.** All five are one practitioner, bringing the vault to **nine of his videos**. **The yield figure measures how much he said that was new to us, not how well-evidenced any of it is** — and the triage picked the five titles most likely to be dense, so a high number was close to guaranteed. **A concentration warning is now carried on every page that cites him.**
  2. **⚠️⚠️ THE BEST MECHANISM IN THE ROUND EXPLAINS A FAILURE THIS REPO HAS ALREADY HAD: git works because THE UNIT IS A LINE, and knowledge work's unit is THE WHOLE FILE.** To change one cell the model rewrites the whole document, so concurrent writes collide, folders fill with duplicates, and every read is the entire file. **This vault's own agent memory records a real two-instance collision; this is its root-cause analysis.** ⚠️ **And it shows this project is already on the architecture he is reaching for — git-backed shared context, plain-text and CSV rather than Excel, deterministic gates rather than a human eyeballing output — largely by default rather than by decision.**
  3. **⚠️⚠️ ONE GENUINELY NEW POSITIONAL FAILURE: "LOST IN THE MIDDLE."** *"With more context, AI reads the start and the finish more reliably, but misses the middle."* **WHERE a rule sits affects whether it is used, not just HOW MUCH context there is.** `AGENTS.md`, `CLAUDE.md` and the long skill files all have middles. **If real, it is an argument for numbered, front-loaded standing rules — and a second justification for the 300-line page target that nobody had stated.** `single-account`, uncited, **recorded as a hypothesis to design against rather than a measured property.**
  4. **⚠️⚠️ COMPLETENESS CHECKING WORKS AGAINST A BASELINE AND FAILS WITHOUT ONE — demonstrated by the same practitioner failing at it.** Asked cold whether anything was missing from an estimate, he got **~50 recommendations, mostly already covered elsewhere.** Asked to check **his own first cut**, it works, because the comparison is bounded. **→ "Is anything missing?" is unreliable; "check MY draft against the source" is not.** *(An independent instance of the manufactured-findings failure already recorded from a DIFFERENT practitioner in `VcBohP2LbNM`.)*
  5. **⚠️ THE ROUND IS MOSTLY VALIDATION, AND THE PAGE SAYS SO IN ITS OPENING.** A router file, a skill library with nested examples, a linked markdown wiki, sub-agents, git-backed context, human gatekeeping — **this repo already had all of it, several in a stronger form.** **The useful output is the vocabulary and the four new items, plus the reassurance that the architecture was not accidental.** ⚠️ **One deliberate divergence recorded rather than followed: he routes live registers to a DATABASE; this project keeps them as CSV in git, which for one person with version control diffs, reviews and reverts without a second system.**
  **Also**: the estimating half of `VrSs8mGI8ss` supplied the round's most adoptable control — **a rate-flagging gate that outputs "I do not know this rate" instead of inventing one**, which is the silently-supplied-values rule applied to prices — and **the scope-gap reconciliation**, which turns the Omsk finisher's hand-written exclusions list into something checkable. **No prices carried** (AU market); no conversion owed; no regulatory content.

- **2026-09-13 — Astra-era modelling batch: 10 videos, 70 new facts, yield 7.0.** No new page; `Agent_Connected_CAD.md` (§6–8), `AI_For_Concept_And_Visualisation.md` (§5–6) and `Drawing_Conventions_From_Practice.md` (§1) extended, plus one routing to `17_Design_and_Ergonomics/`. Triage: [`astra_modelling_batch_20260913.md`](../../_Inbox/planning/astra_modelling_batch_20260913.md). **SIX THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THE LANGUAGE RULE GENERALISES BEYOND RUSSIAN, AND ONE SOURCE PROVED IT.** `-FsHQEYldnQ` has an **ENGLISH title and KOREAN audio** (`ko-orig`). Three of ten are Korean-original, one Russian. **Title language is not a signal of spoken language in ANY language pair** — the rule is written around Russian but the failure is general. **Each was fetched in its verified original; these are the vault's first Korean sources, and one has the author's own manual subtitles, the best transcript quality anywhere here.**
  2. **⚠️⚠️ "COMPUTER USE" IS NOT A FOURTH ARCHITECTURE — IT COLLAPSES INTO TOOL-DRIVING, AND THE PRESENTER SAYS SO.** Driving AutoCAD with **no MCP at all**, the visible GUI stepping is *"actually FOR THE HUMAN"*; it wrote **4,000+ lines of Python** and executed them, and on the second run produced the drawing in one shot with no animation. **The triage predicted a new row on the architecture table; the source removed it.**
  3. **⚠️⚠️ THE REVISION QUESTION IS ANSWERED, AND IT IS WHAT THE ARCHITECTURE DISTINCTION IS FOR.** File generation regenerates everything; **MCP edits in place** — *"modify just that window"*, or *"change all 100 windows"*. **The working method is an ANNOTATED SCREENSHOT plus a plain-language instruction.** The same practitioner **delegated the fiddly MCP installation to the model itself** (7 min 16 s).
  4. **⚠️⚠️ ONE DEFECT CLASS SPANS THREE TOOLS: GENERATED GEOMETRY IS PLACED, NOT ASSOCIATED.** Beams that do not follow their grid when it moves; a dimension **text overridden so it disagrees with the geometry** — a drawing that lies. **The output looks right and nothing holds it right.** → **Ask not "is it correct?" but "is it correct BECAUSE OF anything?"** ⚠️ A purpose-built parametric tool reportedly does maintain it — **the difference is not intelligence, it is whether anything maintains the constraint.**
  5. **⚠️⚠️ AN AGENT SILENTLY CHOOSES A DATUM, AND TWO UNRELATED MODELS CHOSE THE SAME ONE** — the **bottom-left of the wall**, described as coming up *"quite commonly"*. **The silently-supplied-values rule applied to the origin, which is the worst thing to have chosen for you.** ⚠️ **This project has that question open**, and this does not answer it — it names the convention we would be silently fighting.
  6. **⚠️⚠️ TWO TRIAGE TESTS THIS BATCH ESTABLISHED.** **A famous building is not a test of drawing comprehension** — a Villa Savoye demo worked because the model *found the drawings online itself*; **retrieval, not reading**, the same confound as a leaked benchmark. And **a self-generated check is not an independent one** — two models built their own verification artefacts, and one of them printed *"complete"* over a half-finished drawing.
  **Also**: **of ten sources, essentially one measures anything.** The vault's only measured elevation-to-3D fidelity check remains `sujS9Mgveo4`'s 610 against 616 mm; **a visual "near-perfect" is recorded as exactly that throughout.** **Every failure observed across the batch is a BUILDABILITY or SPATIAL-LOGIC failure** — furniture across a stair, a door into an outside bedroom, a round sink where the drawing shows square — **not a drafting failure.** Four sources partially processed and one near-skipped, all saying so. **No prices carried** (Indian, Korean and subscription figures, all rule 2); no conversion owed; **no regulatory content.**

- **2026-09-13 — Bonsai/IFC/Blender batch: 11 videos (10 processed, 1 duplicate), 81 new facts, yield 7.4.** **Two new pages** — `Model_To_Drawing_Pipeline.md`, and `Raster_To_Geometry.md` extracted from `Drawing_Conventions_From_Practice.md` via `tools/split_page.py apply` when that page reached **390 of the 400 backstop** (parity verified: 227 content lines, 0 missing, 41/41 citation IDs). `Drawing_Conventions_From_Practice.md` (§3, §6) and `Quantity_Takeoff_and_Cost_Join.md` (new §5) extended. Triage and channel assessment: [`bonsai_ifc_batch_20260913.md`](../../_Inbox/planning/bonsai_ifc_batch_20260913.md). **SIX THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THIS BATCH IS EVIDENCE ON A DECISION ALREADY TAKEN, NOT CAPABILITY SCOUTING — AND THAT IS NEW FOR THIS FOLDER.** The previous four rounds were general AI sources and were mostly a mirror. **This one is Blender + Bonsai + IFC, the toolchain this project actually runs**, and [`toolchain_gap_analysis_20260908.md`](../../_Inbox/planning/toolchain_gap_analysis_20260908.md) §Kind 3 already marks Bonsai's drawings subsystem **`Adopt`**, with one open question: is it usable headless? **The right value filter for such a batch is "does this change the adopt decision or its open question", and it is much sharper than "is this interesting".**
  2. **⚠️⚠️ THE ADOPT VERDICT IS VINDICATED ON A BETTER GROUND THAN IT WAS ARGUED FROM.** It was argued from *annotation quality*. **The real difference between the two routes is ASSOCIATION**: in the BIM route the drawing is **generated** from the model and regenerating shows the edit; in the mesh route the drawings are **exported meshes**, permanently detached, and a dimension on one cannot know its model changed. **This is the *placed, not associated* defect from the previous round arriving in the drawing layer** — and this project's whole discipline is built on not keeping two copies of one fact.
  3. **⚠️⚠️ BUT THE HEADLESS QUESTION MUST BE RESTATED, BECAUSE THE ASSOCIATION LEAKS IN FOUR PLACES.** In one beginner tutorial: **moving a door does not move its opening** (`shift G`); a parameter change needs an explicit **refresh**; **flipping a door's swing displaces it**; parametric conversion loses materials. **Each is a manual gesture a headless script must know to call, and a missed one yields not an error but a plausible-looking IFC with a door whose opening is elsewhere.** → Ask **"which re-synchronisation steps must a script call explicitly, and what gates the result?"** ⚠️ **And build the opening-coincidence check BEFORE adopting anything.**
  4. **⚠️⚠️ A PLAN IMAGE IS NOT UNIFORMLY SCALED, AND THE MAGNITUDE IS UNCOMFORTABLE.** A practitioner scales a room's width to its printed 6096 mm and finds the perpendicular reads **7728 against a printed 7773 — 45 mm of anisotropic format-fitting compression in the same sheet.** **→ `raster_fidelity.py` fits ONE GLOBAL registration; confirm its residuals are isotropic and spatially uniform, not merely within tolerance on average.** ⚠️⚠️ **And `Geometry_Variance_Study.md` measures −45 to +30 mm and calls it build variance.** 45 mm of sheet distortion is indistinguishable from that. The ±50 mm tolerance is still safe — it absorbs both — **but that study measures "plan-to-reality disagreement", not "construction variance", and the two must not be used interchangeably.** Flagged for the owner.
  5. **⚠️⚠️ A LIVE HAZARD FOUND IN OUR OWN DOCUMENTATION, FROM THE ONLY CONTROLLED EXPERIMENT IN FOUR BATCHES.** The `.blend` and the IFC become **linked**, and *"the shortcut for saving IFC project or saving Blender project is the same"* — **an ordinary `Ctrl+S` writes to the canonical IFC.** `00_Master/How_To_View_Outputs.md` tells the owner to open the project IFC in Blender **to look at it**, and Blender users press Ctrl+S by reflex. **Neither `check_dxf_closure.py` nor `raster_fidelity.py` would catch it — both assert the DXF.** → **Open the IFC read-only for viewing, or hash it in the batch gate** (the frozen-artefact pattern this repo already uses three times).
  6. **⚠️⚠️ A STANDING OPEN ITEM IS NOW SETTLED BY EVIDENCE: WALL DIRECTION.** A second source, in a different tool and a different language, independently gives the rule — *"you do want to make sure that you're drawing in a CLOCKWISE direction… walls have an inside and an outside direction"* — **and supplies the half the Russian source did not: the error propagates into every object hosted on that wall, silently.** Two languages, two tools, same rule: **it is not a tool quirk.** → State it **affirmatively** in `residential-bim-geometry-rules`, and **name which direction**; the sources agree on consistency, not on clockwise.
  **Also**: **a genuine practitioner disagreement recorded unresolved** — one aligns window heads to door heads at 2.1 m as a coordination convention, another sets doors at 2100 and windows at 2200 *"a little lower than the windows"*; neither gives a reason. **The ~1 m plan cut plane is confirmed by two independent Bonsai practitioners**, one of whom hedges *"I guess"* because **the UI does not state it** — so our own plan generation must make it an explicit committed parameter. **⚠️ `xlmbZHIaHJw` is the SEVENTH Justin Geis source across two channels** — concentration flagged on the page and the note; recommend no further Geis sources unless one answers a specifically open question. **No prices carried** (none comparable; North American framing figures flagged and not transferred). **No regulatory content.**

- **2026-09-13 — @IfcArchitect Tier 1: 8 videos, 89 new facts, yield 11.1.** No new page; `Model_To_Drawing_Pipeline.md` substantially extended (§4b, §10–13, open items rewritten), `Drawing_Conventions_From_Practice.md` (§1, §3) and `Quantity_Takeoff_and_Cost_Join.md` (§5) extended. Triage and question-map: [`ifcarchitect_tier1_20260913.md`](../../_Inbox/planning/ifcarchitect_tier1_20260913.md). **SIX THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THE FIRST ROUND IN THIS FOLDER THAT ANSWERED NAMED QUESTIONS RATHER THAN EXTRACTING OPENLY — AND THAT IS THE TRANSFERABLE METHOD.** The channel was picked because its contents mapped onto the open items almost item-for-item, and **the triage wrote the map before fetching.** Four open items closed, two earlier conclusions corrected. ⚠️ **A question a source does not answer is a result too** — and the biggest finding here is a negative.
  2. **⚠️⚠️ THERE IS NO AUTO-DIMENSION, AND THAT CORRECTS THE PREMISE BEHIND THE `Adopt` VERDICT.** A dimension in Bonsai is **a polyline the user snaps vertex by vertex**; text collisions are fixed by shrinking fonts. **Three instances now, across two tools and two presenters.** The gap analysis marks the drawings subsystem `Adopt` **specifically because** hand-rolling fails on colliding dimension chains — **Bonsai does not solve that, it gives it to the human.** **→ Our generator can emit chains at computed offsets from data we already hold; for DIMENSIONS the generative route is the better one.** ⚠️ This does not overturn `Adopt` for the rest of the subsystem.
  3. **⚠️⚠️ BUT TAGS ARE BULK-PLACED AND DATA-BOUND, AND THAT IS WHERE THE VALUE ACTUALLY SITS.** `bulk tag` places one per selected object, each already related to it, and **the text is a `{{ }}` TEMPLATE resolved against the bound object** — **template type-level, value instance-level**, so one edit re-templates every tag and each still shows its own data. **The same binding drives the title blocks.** **→ It answers the `1/2` proportional-dimension question affirmatively AND removes the "needs Bonsai" horn: template substitution over an SVG is a string operation, and we already produce the SVG and hold the data.**
  4. **⚠️⚠️ THE OUTPUT IS SVG STYLED BY CSS — SO THE "TWO ENGINES" FRAMING WAS WRONG.** Confirmed four ways: drawings open in a browser; sheets are `.svg` files in a named directory edited in Inkscape; per-drawing filtering uses a **CSS attribute selector**; and the stylesheet is a literal `.css` file. **Bonsai's output format is our output format.** ⚠️ **And the styling taxonomy is the right one to copy — `cut` / `projection` / `text` / `annotation` / `material`** — because **cut versus projection is the fundamental distinction in architectural drawing**, and it needs no Bonsai. ⚠️ **The "magic strings" of the previous round are simply CSS class names; the earlier characterisation was corrected in place.**
  5. **⚠️⚠️ SHEET ASSEMBLY IS MANUAL DRAG-AND-DROP, SO ADOPTING IT WOULD MEAN LOSING A CAPABILITY WE HAVE.** Bonsai dumps every drawing onto the page — *"this beautiful mess"* — and a human arranges them in Inkscape. **Drawing generation: they are strong and we have nothing. Sheet composition: they are manual and we are already programmatic.** ⚠️ **The round trip is the good part** — manual arrangement survives regeneration — **and that principle is worth copying regardless.** ⚠️⚠️ **One hazard: a CLIP HIDES GEOMETRY, IT DOES NOT REMOVE IT**, which matters here because `vector_extent_oracle.py` reads a PDF's vector content.
  6. **⚠️⚠️ TWO DECISIONS HANDED TO THE OWNER, AND ONE CORRECTION.** **(a) A DETAIL MUST BE MODELLED** — battens, fascia, ceiling, all as real objects — so our ±50 mm single-solid model cannot produce one; choose between modelling at detail resolution, drafting in 2D, or **issuing no details and saying so**. **(b) A DEMOLITION PLAN IS A QUERY** over `Pset_<Class>Common.Status` — **the same three phases we carry as DXF layers, but as standard model data**, strictly better than a layer; ⚠️ **adopt the field per instance, NOT his type-per-phase pattern**, which multiplies the type library. **(c) CORRECTED: the type library does NOT quantise wall thickness** — an exact 220 type takes five minutes, and the earlier inference that a type workflow costs dimensional fidelity was wrong.
  **Also**: **four concrete re-synchronisation steps now recorded** for the restated headless question, including **`create drawing` must precede `create sheets`** or the sheet is silently stale — **a cheap first gate is "no sheet older than its drawings."** **⚠️ Two instances in one round of reaching for a VENDOR EXTENSION where the standard falls short** (a Blender-specific quantity set over `IFC4 base quantities`; `EPset_status` where `Pset_SpaceCommon` has no Status) — **prefer the standard property set, and record why if not.** **⚠️ Four of five styling artefacts live in the application install directory**, which is close to disqualifying on this repo's own terms and means anything adopted must be repo-managed and installed by the build. **⚠️⚠️ ALL EIGHT SOURCES ARE ONE SOUTH AFRICAN ARCHITECT** — he names his national standards twice — **so every convention carries an unstated national default and none of it routes to `16_Legal_and_Regulations/`.** No prices; no regulatory content.

- **2026-09-13 — Dude Blender floor-plan series: 3 videos (one exercise), 29 new facts, yield 9.7.** No new page; `Raster_To_Geometry.md`, `Model_To_Drawing_Pipeline.md` (§14b–14d), `Quantity_Takeoff_and_Cost_Join.md` (§1) and `Drawing_Conventions_From_Practice.md` (§3) extended. **FIVE THINGS WORTH CARRYING:**
  1. **⚠️⚠️ A SIXTH SOURCE ON A COVERED TASK EARNED ITS PLACE ON DISCIPLINE, NOT TECHNIQUE — AND THAT IS THE TRIAGE LESSON.** The modelling content is a fifth restatement and was expected to be. **What was new is that this presenter states his invariants in advance, enumerates failure modes as a checklist, names where he is guessing, and reports defects he cannot explain.** ⚠️ **The value filter should ask "does this source say how it could be WRONG", not only "does it cover a new task."** — and the modelling task is now closed: further floor-plan-in-Blender sources should be declined.
  2. **⚠️⚠️ WALL DIRECTION IS THE FACE NORMAL, AND IT COMES WITH A VISUAL GATE.** Third source, third tool, third region — *"all of the segments facing the same way, and the red side facing the direction to where the wall will gain its thickness."* **Blue outside, red toward the thickness: a whole-model property with a defined pass state, checkable at a glance.** That is the shape of a validator, reached by a practitioner with no validator vocabulary. **And it follows that THE DRAWN LINE IS A WALL FACE, NOT A CENTRELINE** — third independent arrival, matching our own `clear_mm` plus corner-ownership design. ⚠️ **He then declines to align his own vertices to faces because the rounding errors make it pointless — an explicit statement that the tutorial geometry is not dimensionally sound.**
  3. **⚠️⚠️ THE OPENING IS THE FRAGILE RELATIONSHIP IN BOTH FAMILIES, AND THE INVARIANTS ARE NOW WRITTEN OUT.** The mesh route's collection-Boolean *looked* more robust than Bonsai's `shift G`. It is not: **the cutter silently vanishes on duplicate, rotating a door ejects it from the collection, and anything else dropped in that collection is subtracted from the walls.** **BIM: the void does not follow the element. MESH: the cutter is lost, ejected, or joined by an impostor. In every case the wall silently has no hole and the model looks plausible.** → His troubleshooting checklist **is** the missing opening-coincidence check: *every opening has exactly one cutter*, and *the cutter collection contains only cutters.* **Now a cross-family requirement, not a Bonsai quirk.**
  4. **⚠️⚠️ THE COMMIT POINT, AND WHY OUR ROUTE ESCAPES THE TRADE-OFF.** The mesh route is parametric until you bake the modifier — *"all of this will become real geometry and **there is no way back**"* — and he names the price himself: **"full control over our walls AT THE COST OF FLEXIBILITY."** **Third form of the batch's central theme: the BIM route stays parametric and leaks its associations; the mesh route bakes and loses its parameters.** ⚠️⚠️ **Ours has neither problem, because the parameters live UPSTREAM in `data/canonical/` rather than inside a modifier stack or an element type. Neither hand route can have both, and that is worth stating explicitly.**
  5. **⚠️⚠️ A MATERIAL SLOT IS THE `resource_role` / `product_id` SPLIT MADE EXPLICIT — THE CLEANEST ARRIVAL YET.** Faces are assigned to a **slot**; the slot holds a **material**; re-point the slot and every face updates with no change to geometry. **Third independent arrival at the split, and the only one where the indirection is an actual object rather than an implication of a shared name.** **It is exactly the owner's stated primary cost requirement** — re-point *"the floor tile in room 04"* at a different product without touching the model — **and it is what Quantifier Pro's material-keyed costing cannot express.** → **Our cost engine should carry the SLOT, not the material.** ⚠️ **And the floor is subdivided by MATERIAL REGION, not by room**, which our per-room `room_rollouts.csv` does not currently reflect: a closet sharing the bedroom floor is not a separate priced line.
  **Also**: ***"edit one, edit all"* is shown to be a property of INSTANCED GEOMETRY, not a BIM concept** — Blender's `Append (Reuse Data)` shares a datablock and editing one edits all, with the trade-off finally quantified (face count against editability); fifth and sixth instances of the theme. **⚠️ Merge-by-distance appears as the doubled-vertex check for the SECOND time from an unrelated presenter** — community-standard practice **carrying the same flaw both times: it deduplicates and reports in one operation, so a non-zero result silently fixes the defect instead of refusing it.** **⚠️ A unit conversion is a derived figure and carries its own error** — he converts imperial to metric by hand and still attributes a visible gap in the trace to it, which is instructive against the source that delegated the same conversion to an LLM unchecked. **⚠️ A standardised fixture must not be scaled to fit** — a WC is a fixed size, a bath is a product choice. **⚠️ False Color as a thresholded stand-in for an aesthetic judgement**, and **outline strokes added because a render loses the edges a drawing depends on** — the presentation-pass rule from the other direction. **`promotional_ratio: medium`** (an announced ad break, a paid asset kit); no product verdict routed. **No prices, no figures transferred** (North American plan, hand-converted); no regulatory content.

- **2026-09-14 — DataDrivenConstruction Tier 1: 4 videos, 35 new facts.** **New page `Requirements_And_Validation.md`**; `Quantity_Takeoff_and_Cost_Join.md` extended with §8. Triage: [`datadrivenconstruction_channel_triage_20260914.md`](../../_Inbox/planning/datadrivenconstruction_channel_triage_20260914.md). **FOUR THINGS WORTH CARRYING:**
  1. **⚠️⚠️ THE QUANTITY→PRICE JOIN IS FINALLY DEMONSTRATED BY A SOURCE, AND THE SHAPE IS THE TAKEAWAY, NOT THE TOOL.** `Quantity_Takeoff_and_Cost_Join.md` has opened since 2026-09-08 with the statement that every source *“derives quantities automatically and prices nothing automatically”*. **This one prices.** The transferable idea is that **a BOQ line is a SINK fed by several quantity sources** — model elements, a PDF take-off, a pivot table — all converging on one line type. **Our cost engine will have at least two sources and they should converge the same way rather than growing two parallel schemas.** ⚠️⚠️ **And its line carries region, currency and a regional factor but NO PRICE DATE — so their schema would fail our own standing rule 2. Copy the shape, add the date.**
  2. **⚠️⚠️ THE RISKIEST STEP IN A COST JOIN IS THE MATCH, AND NOBODY MEASURES IT.** 215 priced positions across 88 MasterFormat sections, every line matched by *“a three-level semantic search”*, **with no error rate stated**. **A wrong match does not fail — it produces a priced line that looks right.** → **If we build the join, the gate is not “did it match” but “is the match right”, and the cheapest honest version is the rate-flagging gate the page already records: output *“I do not know this rate”* instead of inventing one.** ⚠️ **Same source claims a PHOTO becomes priced positions in seconds, with no verification step — recorded as a claim, and it runs straight into our rule that a visual is for agreeing what is wanted, never for deriving a number.**
  3. **⚠️⚠️ A SOURCE DIRECTLY CHALLENGES A DECISION WE HAVE ALREADY TAKEN — AND IT IS LOGGED AS AN OPEN ITEM, NOT ACTED ON.** The gap analysis records an `Adopt` on authoring a project `.ids`. This source shows **20 rules in eight formats all reducing to ENTITY / ATTRIBUTE / CONSTRAINT**, and validates one model against JSON and against Solibri for **identical results** — *“the format is unnecessary complexity”*. ⚠️⚠️ **The caveat halves it: the same agent wrote both parsers, so it shows the formats are interchangeable TO THAT AGENT, not that a real Solibri run would agree.** **→ Recommendation for the owner: write the rules as a three-column CSV first, because that is the load-bearing part; emit `.ids` later if a counterparty ever needs it.** ⚠️ **And note what does NOT obviously survive three columns — cardinality, applicability facets, optional-versus-required.**
  4. **⚠️⚠️ THE VIDEO THAT LOOKED CLOSEST TO OUR GAP DOES NOT CLOSE IT.** The owner-supplied `ryJxOanNJVQ` — one plain-English instruction to an AI agent, 214 line items in ~2.5 minutes with no CAD installed — **produces counts, areas and volumes and NO cost, no rate and no BOQ.** The 12-minute ERP video does. **→ A triage lesson: the most-viewed video on a channel is not the one that answers your question.** ⚠️ **The durable artefact there is the reusable SCRIPT, not the answer** — a pattern this folder now records from several agent sources.
  **Also**: **a “manual” subtitle track can be machine-generated and merely uploader-accepted** — `ryJxOanNJVQ` has no `-orig` at all and its manual `en` renders *Claude Code* as “Cloud Code”; **the tell is errors on proper nouns.** **⚠️ An arithmetic slip went out uncorrected in a video about validation** (3,235 + 1,343 = 4,578, narrated as “45,578”). **⚠️ `QBaH8oBsPpM` is titled *PRACTICAL RAG and LLM SOLUTIONS* and contains no RAG** — no retrieval, no embeddings, no vector store — and carries the channel's only accuracy claim, *“ChatGPT outputs the result with 100% accuracy”*, unfalsifiable as stated and not recorded as a figure. **⚠️ A Revit→XLSX converter and a 5-million-element Kaggle corpus were read and declined as out of scope: we have no Revit files and one project.** **No prices transferred — the 55,000-item catalogue carries no year and no Belarus (rule 2). No regulatory content.**
